## Security considerations

When using {props.MFA} as a form of multi-factor authentication, it is recommended to set up at least one additional multi-factor authentication method as a backup. Should the {props.MFA} service experience downtime, this measure guarantees that users can continue to access their accounts despite the malfunction of one authentication method.
