---
title: Migrate Metadata
description: Learn how to migrate metadata encryption in Passbolt
sidebar_position: 3
---

import Figure from '@site/src/components/Figure/Figure';

# Migrate Metadata

This section enables administrators to migrate existing resource metadata from legacy cleartext format to encrypted format.

<Figure
  src="/img/help/2025/06/migrate-metadata.png"
  alt="Metadata migration interface"
  caption="Metadata migration interface in Passbolt"
/>

## Configuration Options

### Items to Migrate

Toggle whether to include resource metadata in the migration. When enabled, the following fields will be encrypted:

- **Name**
- **Username**
- **URI**
- **Cleartext Description**

If disabled, these fields remain in cleartext form. Passwords are always encrypted regardless of this setting.

### Migration Scope

Choose which types of resources to include in the migration:

- **All content** - Includes both shared and personal resources.
- **Shared content only** - Only shared resources are migrated.

## Important Considerations

Before initiating migration, ensure:
- Encrypted metadata is enabled in [Encrypted Metadata](./encrypted-metadata.mdx)
- All users who need access to migrated resources have received the shared metadata key (see [Metadata Key](/admin/resource-types/metadata-key/))

:::tip Best Practice
Always backup your database before migration. Metadata migration cannot be easily reversed.
:::

:::warning
Migrating content to encrypted metadata might affect integrations that rely on accessing resource metadata in cleartext format.
:::

## Migrating from the command line

The same migrations can be run from the server, which helps when the interface is not practical, for a scripted migration or for a volume you would rather drive from a shell:

```bash
passbolt metadata migrate_resources
passbolt metadata migrate_folders
passbolt metadata migrate_tags
passbolt metadata migrate_all_items
```

Mind the command name: the `metadata` group sits inside the `passbolt` namespace, so the prefix is required. See [Running Commands](/hosting/useful-commands/#running-commands) for the full invocation on your installation type.

Each command needs the matching setting to be enabled first, the same requirement the interface reports on this screen. When it is missing, the command stops and points at the setting, for example for tags:

```
To enable, set "allow_creation_of_v5_tags" metadata settings to true via `update_metadata_types_settings` command.
```

:::caution
`migrate_tags` exits with an error when there is no tag to migrate, which is worth knowing if you chain these commands in a script. And unlike the post-upgrade backfill commands, it is not documented as safe to run twice, so prefer the migration status shown on this screen over re-running it blindly.
:::

 