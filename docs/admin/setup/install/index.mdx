---
title: Install Passbolt CE or PRO
sidebar_label: Install
description: Install Passbolt CE or Passbolt PRO
---

import DistributionCard from "@site/src/components/DistributionCard/DistributionCard.js";
import distributions from "@site/data/distributions.json";
import styles from "@site/src/components/DistributionCard/DistributionCard.module.css";

export const DistributionCardWrapper = () => {
  return (
    <div className={styles.grid}>
      {distributions.map((distribution) => {
        return (
          <DistributionCard
            key={distribution.slug}
            logo={distribution.logo}
            name={distribution.name}
            version={distribution.version}
            links={{
              ...(distribution.ce && {
                ce: `${distribution.slug}?version=ce`,
              }),
              ...(distribution.pro && {
                pro: `${distribution.slug}?version=pro`,
              }),
            }}
          />
        );
      })}
    </div>
  );
};

<DistributionCardWrapper />


