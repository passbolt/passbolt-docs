import Figure from '@site/src/components/Figure/Figure';


The passbolt debian based package currently supports the configuration of nginx. It comes with a default configuration that supports:

  -  Serving passbolt on port 80 (http)
  -  Serving passbolt on port 443 (https)

The following steps will guide you through the option that uses Let’s encrypt method to enable SSL.

<>
{props.productName == 'PRO' && (
  <Figure
    size={{ width: '450px', height: 'auto' }}
    src="https://help.passbolt.com/assets/img/help/2020/05/debian-package/configure_nginx_pro.png"      
    alt="Configure nginx dialog "
    caption="fig. Configure nginx dialog"
  />
  )}
{props.productName == 'CE' && (
  <Figure 
    size={{ width: '450px', height: 'auto' }}
    src="https://help.passbolt.com/assets/img/help/2020/05/debian-package/configure_nginx.png"      
    alt="Configure nginx dialog "
    caption="fig. Configure nginx dialog"
  />
  )}
</>



After choosing yes you will be prompted with the following dialog where you can choose which method you prefer to configure SSL on nginx:
<>
{props.productName == 'PRO' && (
  <Figure
    size={{ width: '450px', height: 'auto' }}
    src="https://help.passbolt.com/assets/img/help/2020/05/debian-package/nginx_choices_pro.png"
    alt="nginx SSL dialog"
    caption="fig. nginx SSL dialog"
  />
  )}
{props.productName == 'CE' && (
  <Figure 
    size={{ width: '450px', height: 'auto' }}
    src="https://help.passbolt.com/assets/img/help/2020/05/debian-package/nginx_choices.png"
   alt="nginx SSL dialog"
    caption="fig. nginx SSL dialog"
  />
  )}
</>


You will now need to introduce the name of the domain name assinged to your server:
<>
{props.productName == 'PRO' && (
  <Figure
    size={{ width: '450px', height: 'auto' }}
    src="https://help.passbolt.com/assets/img/help/2020/05/debian-package/nginx_domain_pro.png"
    alt="nginx domain name"
    caption="fig. nginx domain name"
  />
  )}
{props.productName == 'CE' && (
  <Figure 
    size={{ width: '450px', height: 'auto' }}
    src="https://help.passbolt.com/assets/img/help/2020/05/debian-package/nginx_domain.png"
    alt="nginx domain name"
    caption="fig. nginx domain name"
  />
  )}
</>



Finally you will need to provide an email address for Let’s encrypt to notify you for renewals and other admin info:
<>
{props.productName == 'PRO' && (
  <Figure
    size={{ width: '450px', height: 'auto' }}
    src="https://help.passbolt.com/assets/img/help/2020/05/debian-package/lets_encrypt_email_pro.png"
    alt="lets encrypt admin email"
    caption="fig. lets encrypt admin email"
  />
  )}
{props.productName == 'CE' && (
  <Figure 
    size={{ width: '450px', height: 'auto' }}
  src="https://help.passbolt.com/assets/img/help/2020/05/debian-package/lets_encrypt_email.png"
  alt="lets encrypt admin email"
  caption="fig. lets encrypt admin email"
  />
  )}
</>