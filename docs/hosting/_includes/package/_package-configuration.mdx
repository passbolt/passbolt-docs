import ConfigureDebianPackage from './_configure-debian-package-mariadb.mdx';

<div>
  {(props.distributionLabel === 'Debian' ||
    props.distributionLabel === 'Ubuntu' ||
    props.distributionLabel === 'Raspberry') && (
    <>
      <ConfigureDebianPackage
        distributionLabel={'Debian'}
        databaseEngine={'mariadb'}
      />
      <h3>Configure nginx for serving HTTPS</h3>
      Depending on your needs there are two different options to setup nginx and
      SSL using the {props.distributionLabel} package:
      <ul>
          <li>
              <a href={`/docs/hosting/configure/HTTPS/${props.productName}/debian-auto`}> Auto (Using Let's Encrypt) </a>
          </li>
          <li>
              <a href={`/docs/hosting/configure/HTTPS/${props.productName}/debian-manual`}> Manual (Using user-provided SSL certificates) </a>
          </li>
      </ul>
    </>
  )}
</div>
