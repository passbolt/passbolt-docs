import AdmonitionsInstallPrerequisites  from '/docs/hosting/_includes/package/_prerequisites_ad.mdx';
import Link from '@docusaurus/Link';

 ## Prerequisites

<div>
    For this tutorial, you will need:
    <ul>
        {props.distributionLabel === 'Raspberry' ? (
            <>
                <li>Any Raspberry Pi model that supports 64-bit OS (e.g., Raspberry Pi 3, 4, or later).</li>
                <li>A 64-bit version of Raspberry Pi OS Lite (formerly Raspbian) or any OS based on Debian 13 Trixie. Raspberry Pi has discontinued 32-bit support for some configurations, so a 64-bit OS is required for this installation.</li>
            </>
        ) : (
            <li>A minimal {props.distributionLabel} {props.distributionVersion} server.</li>
        )}
        {(props.type === 'updatePackage') ? (
            <>
                <li>Passbolt {props.distributionLabel} {props.distributionVersion} package installed.</li>
            </>
        ): (
            <>
                <li>A domain / host name pointing to your server, or at least being able to reach your server through a static IP address.</li>
                <li>A working SMTP server for email notifications</li>
                <li>A working NTP service to avoid GPG authentication issues</li>
            </>
        )}
    </ul>
</div>



The recommended server requirements are:
- 2 cores
- 2GB of RAM

FAQ pages:

<div>
    <ul>
        {props.distributionLabel !== 'Debian' && props.distributionLabel !== 'Ubuntu' && props.distributionLabel !== 'Raspberry' && (
            <li><Link to={"/docs/hosting/faq/why-should-I-install-haveged-on-vms"}>Considerations about entropy</Link></li>
        )}
    <li><Link to={"/docs/hosting/configure/ntp"}>Set up NTP</Link></li>
    <li><Link to={"/docs/hosting/configure/firewall-rules"}>Firewall rules</Link></li>
    </ul>
</div>

{props.type !== 'updatePackage' ? <AdmonitionsInstallPrerequisites /> : null}
