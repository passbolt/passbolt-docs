---
title: Mobile
description: How to create a TOTP on Mobile application
slug: mobile
---

import Figure from '/src/components/Figure/Figure';
import Chips from "/src/components/Chips/Chips";

<div style={{display: "flex", gap: "1rem"}}>
  <Chips link="https://www.passbolt.com/ce/docker" isUnderMainTitle={true}>CE</Chips>
  <Chips link="https://www.passbolt.com/pricing/pro" isUnderMainTitle={true}>Pro</Chips>
  <Chips link="https://www.passbolt.com/pricing/cloud" isUnderMainTitle={true}>Cloud</Chips>
</div>

# How to create a TOTP on mobile

Since version 4.3.0, Passbolt supports creation of TOTP (Time-based One Time Password).

TOTP is a mechanism that generates a unique and temporary password based on the current time. This dynamic code can be used on its own or in combination with a static password, offering an additional layer of security compared to traditional password-only systems.

* [iOS](#ios)
* [Android](#android)

## iOS

On the iOS application, there is a new section called “TOTP”

<Figure
  size={{
    width: '400px',
    height: 'auto',
  }}
  src={
    '/img/help/2023/10/totp_menu.png'
  }
  caption="fig. iOS - Empty TOTP"
  alt="iOS - Empty TOTP"
/>

In order to create a new TOTP, you’d need to click on “Create”

<Figure
  size={{
    width: '400px',
    height: 'auto',
  }}
  src={
    '/img/help/2023/10/totp_creation.png'
  }
  caption="fig. iOS - TOTP Creation"
  alt="iOS - TOTP Creation"
/>

That will open a menu that will let you choose between scanning a QR code or create a TOTP manually, for this tutorial we assume that you’d need to create it manually.

For the TOTP manual creation, you will have to fill three fields:

1. Name, which is the label of the resource
2. URL, which is the fullBaseUrl of the resource
3. Secret, the secret from the TOTP provider

<Figure
  size={{
    width: '400px',
    height: 'auto',
  }}
  src={
    '/img/help/2023/10/totp_manual_configuration.png'
  }
  caption="fig. iOS - TOTP Configuration"
  alt="iOS - TOTP Configuration"
/>

You do have the possibility to link this TOTP to an existing password but that’s optional. You can also create a standalone TOTP instead.

<Figure
  size={{
    width: '400px',
    height: 'auto',
  }}
  src={
    '/img/help/2023/10/totp_linked_to_password.png'
  }
  caption="fig. iOS - Link TOTP to an existing password"
  alt="iOS - Link TOTP to an existing password"
/>

There is also an advanced settings part in order to adjust the **expiry, length and algorithm**

:::warning
Advanced settings have to match the TOTP provider settings otherwise it won’t work.
:::

<Figure
  size={{
    width: '400px',
    height: 'auto',
  }}
  src={
    '/img/help/2023/10/totp_advanced_settings.png'
  }
  caption="fig. iOS - TOTP Advanced Settings"
  alt="iOS - TOTP Advanced Settings"
/>

Once created, you will see a success message “TOTP has been created.” then you will be able to preview the TOTP code when you need it.

<Figure
  size={{
    width: '400px',
    height: 'auto',
  }}
  src={
    '/img/help/2023/10/totp_code_preview.png'
  }
  caption="fig. iOS - TOTP Preview"
  alt="iOS - TOTP Preview"
/>

## Android

On the Android application, there will be a new section called “TOTP”

<Figure
  size={{
    width: '400px',
    height: 'auto',
  }}
  src={
    '/img/help/2023/10/totp_android_empty_totp.jpg'
  }
  caption="fig. Android - Empty TOTP"
  alt="Android - Empty TOTP"
/>

In order to create a new TOTP, you’d need to click on “+” icon

<Figure
  size={{
    width: '400px',
    height: 'auto',
  }}
  src={
    '/img/help/2023/10/totp_android_creation.jpg'
  }
  caption="fig. Android - TOTP Creation"
  alt="Android - TOTP Creation"
/>

That will open a menu that will let you choose between scanning a QR code or create a TOTP manually, for this tutorial we assume that you’d need to create it manually.

For the TOTP manual creation, you will have to fill three fields:

1. Name, which is the label of the resource
2. URL, which is the fullBaseUrl of the resource
3. Secret, the secret from the TOTP provider

<Figure
  size={{
    width: '400px',
    height: 'auto',
  }}
  src={
    '/img/help/2023/10/totp_android_configuration.jpg'
  }
  caption="fig. Android - TOTP Configuration"
  alt="Android - TOTP Configuration"
/>

You do have the possibility to link this TOTP to an existing password but that’s optional. You can also create a standalone TOTP instead.

<Figure
  size={{
    width: '400px',
    height: 'auto',
  }}
  src={
    '/img/help/2023/10/totp_android_link_to_password.jpg'
  }
  caption="fig. Android - Link TOTP to an existing password"
  alt="Android - Link TOTP to an existing password"
/>

There is also an advanced settings part in order to adjust the  **expiry, length and algorithm**

:::warning
Advanced settings have to match the TOTP provider settings otherwise it won’t work.
:::

<Figure
  size={{
    width: '400px',
    height: 'auto',
  }}
  src={
    '/img/help/2023/10/totp_android_advanced_settings.jpg'
  }
  caption="fig. Android - TOTP Advanced Settings"
  alt="Android - TOTP Advanced Settings"
/>

Once created, you will see a success message then you will be able to preview the TOTP code when you need it.

<Figure
  size={{
    width: '400px',
    height: 'auto',
  }}
  src={
    '/img/help/2023/10/totp_android_code_preview.jpg'
  }
  caption="fig. Android - TOTP Preview"
  alt="Android - TOTP Preview"
/>
