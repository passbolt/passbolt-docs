folderAdd:
  description: The folder you want to create
  required: true
  content:
    application/json:
      schema:
        $ref: "../schemas/e2eeObjects.yml#/e2eeMetadataBased"
      examples:
        $ref: "../examples/folders.yml#/folderAdd/request"

folderUpdate:
  description: The folder you want to update
  required: true
  content:
    application/json:
      schema:
        $ref: "../schemas/e2eeObjects.yml#/e2eeMetadataBased"
      examples:
        $ref: "../examples/folders.yml#/folderUpdate/request"
