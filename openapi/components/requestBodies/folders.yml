folderAdd:
  description: The folder you want to create
  required: true
  content:
    application/json:
      schema:
        $ref: "../schemas/folders.yml#/folderAdd"
      examples:
        $ref: "../examples/folders.yml#/folderAdd/request"

folderEdit:
  description: The folder you want to update
  required: true
  content:
    application/json:
      schema:
        $ref: "../schemas/folders.yml#/folderEdit"
      examples:
        $ref: "../examples/folders.yml#/folderEdit/request"
