resourceAddAndUpdate:
  description: The resource you want to create or update
  required: true
  content:
    application/json:
      schema:
        $ref: "../schemas/resources.yml#/resourceAddAndUpdate"
      examples:
        $ref: "../examples/resources.yml#/resourceAddAndUpdate/request"
