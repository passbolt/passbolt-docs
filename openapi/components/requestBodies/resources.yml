resourceCreate:
  description: The resource you want to create
  required: true
  content:
    application/json:
      schema:
        $ref: "../schemas/resources.yml#/resourceCreate"
      examples:
        $ref: "../examples/resources.yml#/resourceCreate/request"

resourceUpdate:
  description: The resource you want to update
  required: true
  content:
    application/json:
      schema:
        $ref: "../schemas/resources.yml#/resourceUpdate"
      examples:
        $ref: "../examples/resources.yml#/resourceUpdate/request"
