resourceAdd:
  description: The resource you want to create
  required: true
  content:
    application/json:
      schema:
        $ref: "../schemas/resources.yml#/resourceAdd"
      examples:
        $ref: "../examples/resources.yml#/resourceAdd/request"

resourceEdit:
  description: The resource you want to update
  required: true
  content:
    application/json:
      schema:
        $ref: "../schemas/resources.yml#/resourceEdit"
      examples:
        $ref: "../examples/resources.yml#/resourceEdit/request"
