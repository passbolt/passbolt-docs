userUpdate:
  description: The user you want to update
  required: true
  content:
    application/json:
      schema:
        $ref: "../schemas/users.yml#/userUpdate"
      examples:
        $ref: "../examples/users.yml#/userUpdate/request"
