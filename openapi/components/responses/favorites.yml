add:
  description: "Operation is successful"
  content:
    application/json:
      schema:
        type: object
        required:
          - header
          - body
        properties:
          header:
            $ref: "../schemas.yml#/header"
          body:
            $ref: "../schemas/favorites.yml#/favorite"
      examples:
        $ref: "../examples/favorites.yml#/favoriteAdd/response"
