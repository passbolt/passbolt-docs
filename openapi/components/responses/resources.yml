show:
  description: "Operation is successful"
  content:
    application/json:
      schema:
        type: object
        required:
          - header
          - body
        properties:
          header:
            $ref: "../schemas.yml#/header"
          body:
            $ref: "../schemas/resources.yml#/resourceIndexShow"
      examples:
        $ref: "../examples/resources.yml#/resourceShow/response"

createUpdate:
  description: "Operation is successful"
  content:
    application/json:
      schema:
        type: object
        required:
          - header
          - body
        properties:
          header:
            $ref: "../schemas.yml#/header"
          body:
            $ref: "../schemas/resources.yml#/resourceIndexShow"
      examples:
        $ref: "../examples/resources.yml#/resourceCreateUpdate/response"

index:
  description: "Operation is successful"
  content:
    application/json:
      schema:
        type: object
        required:
          - header
          - body
        properties:
          header:
            $ref: "../schemas.yml#/headerWithPagination"
          body:
            type: "array"
            items:
              $ref: "../schemas/resources.yml#/resourceIndexShow"
      examples:
        $ref: "../examples/resources.yml#/resourcesIndex/response"
