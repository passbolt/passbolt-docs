view:
  description: "Operation is successful"
  content:
    application/json:
      schema:
        type: object
        required:
          - header
          - body
        properties:
          header:
            $ref: "../schemas.yml#/header"
          body:
            $ref: "../schemas/secrets.yml#/secretIndex"
      examples:
        $ref: "../examples/secrets.yml#/secretView/response"
