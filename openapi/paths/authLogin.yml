post:
  summary: "Log in."
  operationId: "authLogin"
  security: []
  x-codeSamples:
    $ref: "../components/codeSamples/authGpg.yml#/login"
  tags:
    - "Authentication (GPG)"
  requestBody:
    $ref: "../components/requestBodies/authGpg.yml#/login"
  responses:
    200:
      $ref: "../components/responses/authGpg.yml#/login"
    400:
      $ref: "../components/responses.yml#/badRequest"
