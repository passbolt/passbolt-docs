get:
  summary: "Check if Passbolt is up."
  operationId: "viewHealthcheckStatus"
  security: []
  x-codeSamples:
    $ref: "../components/codeSamples/healthcheck.yml#/status"
  tags:
    - "Healthcheck"
  responses:
    200:
      $ref: "../components/responses/healthcheck.yml#/status"
